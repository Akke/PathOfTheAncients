# Project Conventions

## Technical conventions

- Panorama and other client-side scripts use vanilla JavaScript (`.js`).
- Server-side gameplay scripts use Lua (`.lua`).
- Do not introduce TypeScript, transpilation, or an additional script build step unless the user explicitly changes this rule.
